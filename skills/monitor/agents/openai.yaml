interface:
  display_name: "Monitor"
  short_description: "Feedback-aware local-first PR monitor"
  default_prompt: "Use $monitor to watch the current PR, refresh comments and reviews, get Codex clean, run local test-coded-tests, create one empty Trigger #CI commit when authorized, and continue monitoring until stable or blocked."
