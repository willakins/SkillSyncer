interface:
  display_name: "Publish Draft and Monitor"
  short_description: "Publish then gated monitor"
  default_prompt: "Use $publish-draft-and-monitor to publish this branch as a draft PR, clean it up, mark it ready, then hand off to $monitor for Codex, GitHub Actions specs, and a gated empty #CI trigger commit."
