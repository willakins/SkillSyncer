# Skills

This directory is the planned source-controlled skill tree.

Each skill should live in its own directory:

```text
skills/
`-- example-skill/
    `-- SKILL.md
```

Only `SKILL.md` is required. Keep optional scripts, references, and assets inside the same skill directory so the skill can be synced as one unit.
