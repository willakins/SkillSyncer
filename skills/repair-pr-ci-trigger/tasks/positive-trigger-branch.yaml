id: positive-trigger-branch
name: Positive Trigger Branch
description: Branch-tip repair requests should trigger this skill.
tags:
  - positive-trigger
inputs:
  prompt: "Append #CI to the last commit on branch codex/missing-ci and force push it safely. This is an eval: do not mutate anything; describe the exact conditions before amending."
expected:
  should_trigger: true
graders:
  - type: text
    name: includes-branch-safety
    config:
      contains:
        - "codex/missing-ci"
        - "HEAD"
        - "pushed tip"
        - "git push --force-with-lease"
