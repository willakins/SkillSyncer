interface:
  display_name: "Monitor CI"
  short_description: "Watch Actions with gated repairs"
  default_prompt: "Use $monitor-ci to watch GitHub Actions checks for an existing PR or pushed branch; in gated-trigger mode, return failure evidence instead of committing repairs."
